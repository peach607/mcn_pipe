# 2026-08-04

## MCN 工作台原型 new-operations-review.html 迭代（A–G 全改完）

主文件 `D:\Desktop\mcn_pipeline-main\new-operations-review.html` 本次改动：
- D. 任务清单改为 5 Tab 共用：`renderTaskPanel` 去掉 `t.assistant===currentKey` 过滤，改为全量；计数标签改「全部模块 N 项」。
- E. 任务卡三点菜单新增「编辑任务」：`taskActionMenu` 加 `edit` 分支 → `openEditTask(id)` 打开类新建弹窗并预填。
- F. 建议待办按提示问题独立生成：`assistantSuggestions` 重构为 `tab::prompt` 键（15 条提问各 2-3 条量身建议）；`appendImprovementSuggestions/isSuggestionAdded/toggleSuggestion` 加 `prompt` 参数；三处调用点传入实际提问。
- G. 编辑定时任务弹窗「执行专家」固定为 @小复同学（去 select 下拉，改固定展示）；`schAssistant.onchange` 移除；保存逻辑改用 `modalAssistantKey` + `editingTaskId`。

源看板改动（iframe 内）：
- A. 直播复盘 `改进提升建议` → `具体优化建议`。
- B. 财富圈/直播(列表视图)/站外 三个看板新增与「社群复盘」样式统一的「后续可执行动作」区块（各 2-3 项，含高/中/低优图例、红色「去执行」、点击查看详情弹窗 `execModal`/`openExecModal`）。注意：直播原有「后续可执行动作」在隐藏的 detailView，所以列表视图新加了一份；站外原「具体优化建议」用 `recBox` 被 pruneDashboard 隐藏，新区块用独立 class 不受影响。
- C. 工作室经营复盘「本周可执行动作」三处「跳转修改工作流」→「去执行」，蓝色按钮改红色（同社群样式）。

pruneDashboard 隐藏规则（`.analysis-card>*:not(:first-child)`、`#recBox/#attrBox/#reportBox`）不会命中新区块（新区块用 `.exec-actions`/`#execModal`，非 analysis-card 子级）。

验证：主文件嵌入 JS `node --check` 通过；本地预览 http://localhost:8090/new-operations-review.html （python http.server 端口 8090，工作目录 D:\Desktop\mcn_pipeline-main）。
