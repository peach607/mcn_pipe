# 2026-08-05

## indexnew.html 首页卡片过空修复
- 问题：三张卡片被两处 `.card{min-height}` 强制拉高（第一处 604px、第二处 620px），实际内容约 500px，底部留空。
- 修复：移除两处 min-height，卡片改为按内容自然撑开，靠网格 `align-items:stretch` 等高对齐。
- 首页三卡片为：今日任务 / 数字分身 / 复盘助手。

## 首页「今日任务」例子对齐 AI执行版
- 用户要求首页今日任务卡片的例子与 `今日任务_AI执行版.html` 一致。
- AI执行版时间线真实任务（财经内容运营类）：盘前策略、直播提纲与素材、半导体异动解读、盘中快评、客户回复方案、今日内容数据报告；状态含 已完成/待确认/生成中。
- 将首页 4 条示例替换为：盘前策略(已完成 09:00)、直播提纲与素材(待处理 09:30)、半导体异动解读(已完成 11:30)、盘中快评(进行中 13:15)；统计改为 待处理1/进行中1/已完成2，进度 50%（同步改 .progress b 宽度 60%→50%）。

## 首页右上角头像对齐导航栏
- AI执行版导航栏头像：`pictures/主1.jpg`（profile 区 `<img alt="头像" src="pictures/主1.jpg">`）。
- 首页头像原本是内联 base64 图，已用脚本替换为同一张 `pictures/主1.jpg`（正则替换 `<div class="avatar"><img src="data:image/...">`）。

## 数字分身人像替换为内容3.png
- 用户通过截图指出首页「数字分身」卡片的人像需要替换。
- 已将该人像从 base64 数据图替换为本地图片 `pictures/内容3.png`（正则替换 `<img class="twin-art" src="data:image/...">`）。

## 首页右上角头像下移 0.8cm
- 用户截图指出右上角头像需要向下移动 0.8cm。
- 在首页第二段样式 `.avatar{width:34px;height:34px}` 上增加 `transform:translateY(0.8cm)`，仅视觉上平移，不影响 `.top-actions` 布局。
- 后续反馈：仅头像下移导致头像与铃铛错位。调整为将 `.top-actions` 整体下移 0.8cm（`transform:translateY(0.8cm)`），铃铛与头像保持对齐。

## 数字分身人像底部截断修复
- 用户截图反馈人像底部被截断得奇怪。
- 原因：`.twin-art-wrap` 的 `mask-image` 在底部 90%-100% 做透明渐变，加上 `::after` 40px 白色渐变遮罩，把圆形图片底部"溶掉"。
- 修复：移除 `.twin-art-wrap` 的 `mask-image`，并把 `::after` 高度设为 0，取消底部遮罩，让人像完整显示到底。
- 后续反馈：移除遮罩后，由于容器仍是竖版矩形（180×226）且 `object-fit:cover`，圆形图片仍被容器底部/左右截断。调整为将 `.twin-art-wrap` 与 `.twin-art` 改为 180×180 正方形并设 `border-radius:50%`，让人像完整呈现为圆形。

## 数字分身人像左移 1cm 上移 1cm
- 用户要求数字分身卡片那张圆形头像向左 1cm、向上 1cm。
- 在基础样式 `.twin-art-wrap`（indexnew.html 第 163 行）追加 `transform:translate(-1cm,-1cm)`，负值表示左/上移动。
- 此规则跨视窗生效，紧凑视窗的覆盖样式（293 行）未重声明 transform，因此也会跟着偏移。

## 今日任务卡片副标题改为"每日SOP · AI 自动推进"
- 用户指定首页「今日任务」卡片小字用"每日SOP AI 自动推进"。
- 已在 `indexnew.html` 把刚恢复的 `<div class="subtitle">今日任务 · 实时更新</div>` 改为 `每日SOP · AI 自动推进`（加了分隔点以贴合卡片两段式风格）。

## 今日任务页去掉「早上好张三」问候、内容上移
- 用户要求从 `首页_今日任务演化版.html` 看到的页面去掉问候语"早上好，张三 👋"，并让下方内容上移。
- 问候语实际写在被嵌入的子页面 `今日任务_AI执行版.html`（原 `<header class="greeting">`，高 88px）。已整体删除该 header，`<main>` 直接接 `<section class="board">`，内容整体上移。
- 注意：`今日任务_SOP版.html` 仍有同名问候（第 968 行），本次未改；如需要一并去掉再处理。

## 工作室动态去掉张三/李四两条
- 用户截图反馈删除「张三 完成了 2 项内容工作 10:25」「李四 跟进了 3 条新线索 10:40」两条。
- 已在 `今日任务_AI执行版.html` 工作室动态区删掉这两条 `<div class="activity-row">`，列表从「王小北 11:55」开始。

## 首页演化版工具栏去掉说明小字
- 用户要求去掉 `首页_今日任务演化版.html` 工具栏标题下的小字"保留源页面内容 · 已隐藏左侧导航"。
- 已删除 `<div class="toolbar-title">` 内的 `<small>` 元素，仅保留主标题"今日任务 · AI 执行中心"。

## 首页「今日任务」卡片去掉副标题
- 用户截图反馈去掉首页「今日任务」标题下的小字"今日任务 · 实时更新"。
- 已在 `indexnew.html` 第 350 行删除 `<div class="subtitle">今日任务 · 实时更新</div>`，下方统计/任务清单区上移。
- 后续反馈：用户觉得不好看，已恢复原副标题。

## 数字分身页：去掉顶部导航 + 尺寸与今日任务页一致
- 用户要求 `数字分身.html` 去掉最上面的导航（`.header` 品牌栏/搜索/通知/头像），并让"太长了"的页面尺寸与其它页（今日任务_AI执行版.html，整页一屏不滚动、窄屏缩放）一致。
- 导航含长 base64 头像，用脚本正则删除整块 `<header class="header">…</header>`（HTML 删除，CSS/JS 保留无害）；页尾 JS 用 `querySelectorAll('[data-toast]')` 批量绑定且 getElementById 带 `?.` 保护，删除不报错。
- 尺寸对齐：`.main` 的 `overflow:auto` → `hidden`；`.workspace` 去掉 `min-height:700px`；新增 `@media(max-width:1279px){.app{transform-origin:top left;transform:scale(calc(100vw/1280));height:960px}}`，与今日任务页同一缩放手法（≤980 仍走原滚动布局）。
- 注意：`.header/.brand/.search/.icon-btn/.user` 的 CSS 与 720 媒体查询里对它们的引用已成无效代码（不影响显示），未清理。

## 数字分身页：删顶部 page-head + 压缩尺寸让一屏展示
- 用户反馈页面被截断（左右三张卡、中间人像+命令栏都没显示完整），要求把整体高度调整到一屏，并把"最上面的标题行"删掉。
- 顶部 `.page-head` 整块 HTML 删掉（脚本正则删除，含其后换行）；CSS 中 `.page-head` 规则保留为 dead code。
- 关键 CSS 调整（让 1280×960 缩放基线下能装下）：
  - `.main` padding 22px/28px/24px → 14px/20px/16px
  - `.workspace` 加 `min-height:0`，三列 `292/570/328` → `266/560/306`，gap 18→14
  - `.side` gap 15→12
  - 圆环 610px → 480px，光晕外圈 70/145px → 55/115px
  - `.avatar-wrap` 510px → 360px，bottom 98→52px
  - `.assistant-summary` bottom 72→118px（提到人像上方），padding 15/17→11/14，grid 列宽 42→36
  - `.command-bar` 高度 43→38px，bottom 15→10px
  - 浮动消息 padding 11/13→9/11，min-width 164→148
  - `.card-hd` min-height 52→40，padding 0/18→0/14
  - `.card-bd` padding 16/18→12/14
- 缩放机制保持原状：`@media(max-width:1279px){.app{transform-origin:top left;transform:scale(calc(100vw/1280));height:960px}}`，宽屏原 `100vh`。

## 数字分身页：修复 center-stage 结构错乱 + 人像居中
- 用户反馈"第三列被删了、中间空了、截断"。根因：之前多轮脚本误在 `center-stage` 内多插入了 2 个 `</div>`，导致 `center-stage` 提前闭合，后续浮动消息/人像/助理摘要/命令栏跑到网格外，右列渲染错位被挤没，人像也因结构错乱没在中间。
- 修复：用脚本按正则把整段 `<section class="center-stage">…</section>` 重建为结构正确的版本（保留原文字与 base64 人像），4 个浮动消息为 m1/m2/m5/m6（m3/m4 此前已删）。
- 人像 `.avatar-wrap` 由 `bottom:130px`（贴底）改为 `left:50%;top:46%;transform:translate(-50%,-50%)`，即页面正中；尺寸 `min(24vw,220px)/max 32%` → `min(28vw,288px)/max 38%`。结构修复后三列恢复齐全、中间不再空。

## 复盘助手 4 个数据卡重做
- 用户截图提供 4 张数据卡（增量客户/本周收/客户资产/直播时长），要求替换首页"复盘助手"卡片中原本 4 个数据块（`metrics`×2 + `wide-metric`×2）。
- 改动 `indexnew.html`：
  - HTML 401-408 行：删除 `metrics` 和 `wide-metric` 两个块；插入 `.kpi-grid`（2×2 网格，4 个 `.kpi`），数据按截图填：2,860 / 126.4万 / 486.5亿 / 38.5小时；前4周均值/涨幅（绿 pill）+ 平台均值/对比（高=绿 pill，低=红 pill）。
  - CSS 新增 `.kpi-grid`、`.kpi`、`.kpi-label`、`.kpi-value`、`.kpi-row`、`.kpi-avg`、`.kpi-pill.up`（浅绿 #e6f6ec + #0a8554）、`.kpi-pill.down`（浅红 #fdecec + #c8392c）。
  - 紧凑视窗（≤1279）覆盖样式：kpi-grid 间距 8px，kpi 字号同步缩小。
- 保留 hero 区（"今日复盘"+"实时"+机器人图）和底部 `.hot` 热门内容未动。

### KPI 颜色按用户偏好对调 + 删除热门内容行
- 用户明确偏好"高于平台=红、低于平台=绿"（与常规红跌绿涨不同），把 `.kpi-pill.up/.down` 的颜色对调：up 改红 `#fdecec/#c8392c`，down 改绿 `#e6f6ec/#0a8554`。
- 删除 `复盘助手` 卡片底部的 `<div class="hot">`（🔥 热门内容TOP1 / 播放 12.3万 那行）。

### 删除首页两张卡底的"查看"链接
- 删除首页 `indexnew.html` 中两张卡底部的蓝色 card-link：
  - 「今日任务」卡片底部"查看今日任务 >"（原 379 行）
  - 「数字分身」卡片底部"查看更多动态 >"（原 399 行附近）
- 仅移除两个 `<button class="card-link">` HTML，CSS 与 JS 选择器保留（querySelectorAll 选 0 个元素无害）。

### 复盘详情页"本周可执行动作"移到顶部
- `new-operations-review.html` 第 201 行用 `<iframe src="复盘详情/每周数据复盘-v0728-3.html">` 嵌入该复盘页（iframe src 固定，无切换逻辑）。"本周可执行动作"在此嵌入页内。
- 已改嵌入页 `复盘详情/每周数据复盘-v0728-3.html`：把"四、本周可执行动作"整段（原 457-586 行，45 个开/闭 div 平衡）移到页面标题块之后、数据指标卡片之前（现第 69 行起），原第 587 行游离 `</div>` 一并删除以免破坏布局。
- 其余 4 个复盘详情页（直播/社群/站外内容/财富圈）未被 `new-operations-review.html` 嵌入，未改；如用户要统一"可执行动作置顶"，再对这 4 个文件做同样处理。

### 复盘详情页"运营分析"对比卡块移到最顶部
- 继续在嵌入页 `复盘详情/每周数据复盘-v0728-3.html` 调整顺序。用户要求"运营分析对应的卡片放到最上边"。
- "运营分析"区块 = 第 377-457 行（`<h3>运营分析</h3>` + 6 张对比卡 grid `增量客户/本周创收/客户资产/直播时长/企微问答/闭门直播` + 底部 mock 注脚 `<p>`），内部 div 平衡 0。
- 用脚本整段截取并平移到页面标题块（第 67 行）之后、"本周可执行动作"之前，成为整页内容最上方。
- 新顺序：运营分析(69) → 本周可执行动作(152) → 客户与业务指标(284) → 内容展业指标(387) → 运营分析结论(460) → 归因分析 → 具体优化建议。
- 校验：`☊` 图标仅 1 处（无重复块），页面以 `</html>` 正常结尾；整体 div balance +1 为页面既有（平移操作本身不改变 balance，浏览器自动容错）。

### 外壳页 new-operations-review.html 去掉浮动"今日任务"组件 + 右上角头像
- 浮动的"今日任务"组件（`mcn-task-fab` FAB）由共享 JS `mcn-sidebar.js` 的 `initTaskFab()` 在每页加载时渲染。为只在本页关闭、不影响其它页面：
  - 在 `mcn-sidebar.js` 的 `initTaskFab` 顶部加 `if(window.__disableTaskFab)return;`（向后兼容的 opt-in 开关）。
  - 在 `new-operations-review.html` `<head>`（`mcn-sidebar.js` defer 之前）加 `<script>window.__disableTaskFab=true;</script>`。
- 右上角头像块（麦西恩 + "工作室负责人"领导标签，原 198-204 行）整段删除；保留铃铛 `#bellBtn` 按钮。
- 注意：左侧 `#mainRail` 侧边栏账号里的"麦西恩"是另一处，未动（用户只要求右上角）。
- 用户本次打开文件时外壳页被外部改动过（新增 `html.embed-mode` 隐藏 `#mainRail/.mcn-task-fab` 等样式），以最新内容为准。

### 删除复盘页副标题
- 在嵌入页 `复盘详情/每周数据复盘-v0728-3.html` 第 61 行删除 `<p>` 灰字副标题"过去 5 周业务与内容展业数据全景复盘，辅助运营决策"（标题"每周数据复盘"+蓝色 badge 之下那行）。标题块结构仍完整闭合。

### 删除两处"复盘助手"副标题词
- `new-operations-review.html` 第 168 行：删 `<p class="text-xs text-slate-500 mt-0.5">数据复盘 · 优化建议 · 提升效率</p>`（外壳页"复盘助手"标题 h1 下方灰字副标题）。
- `indexnew.html` 第 407 行：删首页复盘助手卡片标题下的 `<div class="subtitle">实时复盘 · 更新于 10:32</div>`（与之前删"今日任务"卡片副标题同一类操作）。复盘助手卡片仍保留 hero 区（今日复盘/实时/4条重点问题待关注）与 2×2 KPI 网格。

### 复盘详情目录统一"涨红跌绿"配色（A股惯例）
- 用户要求 `mcn_pipeline-main/复盘详情/` 下所有 html 的涨跌配色统一为：上涨=红、下跌=绿。
- 每周数据复盘-v0728-3.html：`.trend-up{background:#dcfce7;color:#166534}`(绿) 与 `.trend-down{background:#fee2e2;color:#b91c1c}`(红) 对调 → 涨红跌绿（影响所有 +X%/"高于平台"标签与"低于平台"标签）。
- 直播复盘-v0728-2.html：`.trend-up{color:#dc2626}`/`.trend-down{color:#16a34a}` 本就红/绿正确；仅"上涨原因"标签(text-green-700→text-red-600)、"下跌原因"标签(text-red-600→text-green-600) 对调；表格 ±% 已涨红跌绿。
- 财富圈复盘-v0728.html：新增订阅大数字 `+342`（text-green-600→text-red-600）涨绿改涨红。
- 社群复盘-v0728-2.html：JS `profit.startsWith('+')?'text-red-600':'text-green-600'` 已符合，未改。
- 站外内容数据复盘-v0728.html：无 ±涨跌幅指标（仅互动率等质量阈值用绿好红差），未改。
- 验证脚本确认 5 文件涨跌配色均符合"涨红跌绿"。

## 18:24 复盘助手导航栏精简
- 文件：`mcn_pipeline-main\new-operations-review.html`
- 改动：删掉 `<div class="shrink-0">` 包裹的"复盘助手"标题块（图标+标题，含副标题"数据复盘·优化建议·提升效率"），保留 `<nav id="reviewTabs">` 中 5 个 tab 卡片。
- 父级 `<div class="flex items-center gap-6 min-w-0 flex-1 mr-6">` 保留，因外层 gap-6 现在不再作用于 logo-tab 间；如需更紧凑可后续去掉 mr-6。
- 5 个 tab 按钮本身带 `flex-1`，会自动平分 nav 宽度，不需要额外 CSS 改动。

## 18:30 恢复副标题 + 删铃铛
- indexnew.html：复盘助手卡片的 .review-hero 内、在 .review-title-row 之前补回 `<div class="review-sub">实时复盘 · 更新于 10:32</div>`，并加 `.review-sub{font-size:12px;color:#6b7da0;margin-bottom:6px;letter-spacing:.2px}` 样式。
- new-operations-review.html：删掉 header 右侧的 `#bellBtn` 铃铛按钮（带"2"角标），整组 `<div class="flex items-center gap-4 shrink-0">` 一并删除。
- 用户澄清：之前响应 23 号"数据复盘·优化建议·提升效率 这几个词也删除再复盘助手下面的词也删除"时把"实时复盘 · 更新于 10:32"也删了；现在按用户要求恢复。
