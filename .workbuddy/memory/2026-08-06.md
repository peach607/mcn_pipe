## 09:14 digital-avatars.html 删除分身管理弹窗
- 文件：`mcn_pipeline\digital-avatars.html`
- 改动：删除右上角"分身管理"按钮点击弹出的模态框。HTML 删除第 344 行 `<div class="mask" id="manageMask">…</div>` 整块；JS 删除原 420–423 行（mask 获取 + openManage/closeManage 函数 + openManage/closeManage/cancelManage/saveManage 绑定 + mask 点击关闭）。
- 保留：右上角"分身管理"按钮（#openManage 第315行）仍显示但点击不再弹窗（无绑定，不报错）；toast 提示组件及 showToast 逻辑保留（toggle 切换/指令发送仍使用）；"前往今日任务"按钮保留。
- CSS 中 `.mask/.modal` 等样式未删（无引用、不影响渲染）。

## 09:27 改"自动执行边界"4个开关文案（分身整体管控视角）
- 文件：`mcn_pipeline\digital-avatars.html` 第 328–331 行（`.permission` 四项）。
- 用户要求：不要"分身能力清单"、不要"设置文档腔"，要"分身整体管控"的约束/边界表达。
- 改为用户给定文案：
  1. 允许服务全部客户 / 关闭后，分身仅服务指定客群。
  2. 允许主动联系客户 / 关闭后，分身仅在客户发起咨询时响应。
  3. 允许自动执行任务 / 关闭后，分身仅生成建议，不直接执行。
  4. 高风险操作需人工确认 / 开启后，群发、发布、投资观点等操作必须人工审核。
- 连带：第4项原为关闭态（toggle 无 on），因新文案"开启后必须人工审核"语义要求默认开启，已将第4个开关设为 `toggle on`（前三项本就 on）。模块标题"自动执行边界"+小字"修改即时生效"保留未动。
