# 2026-08-04

## SOP管理_原型图.html 增加交互效果
- 在不改任何原有文案/结构的前提下，给静态原型加了交互层。
- CSS：状态点呼吸灯、节点入场错峰动画、搜索/对话输入框样式、思考打字指示、节点拖拽态与操作浮层、toast 可关闭。
- JS（IIFE，追加在 </body> 前）：左侧导航高亮切换、顶栏搜索实时过滤 SOP 列表、SOP 项选择高亮、模板下载按钮态切换、铃铛清红点+轻晃、对话发送气泡+思考动画+自动回复、节点拖拽排序(含 connector 整体移动与 reindex/normalizeConnectors)、节点菜单(上移/下移/删除)、双击编辑步骤、加一步、顶栏按钮 toast 反馈、toast 自动隐藏。
- 注意：运行依赖「吉祥物.jpg」与 tokens.css，二者需与原文件同目录。

## 修复：节点上移/下移显示异常
- 原 moveUnit 把两个节点都 detach 成 fragment 后再 `u2.before/after(u1)`（u2 已脱离文档 → 无操作，节点消失）；拖拽 drop 误用了 drop target 而非 dragged 节点。
- 改为统一模型：重排后由 `rebuildConnectors()` 依据节点顺序整体重建连接线（除最后一个节点外每个后都有一条线），`moveNodeTo(moving,target,after)` 仅调整节点顺序再重建。`dragNode` 存拖拽源，drop 调用 `moveNodeTo(dragNode, node, after)`。删除节点也走 rebuildConnectors。

## indexnew.html 今日任务卡片优化
- 仅改样式与结构，不动正文语义。
- 排版：扁平表格式行改为卡片式行（flex：彩色序号 badge + 类型 chip + 任务名/时间 body + 右侧状态 tag），左侧 4px 彩色竖条，hover 右移+阴影。
- 类型颜色：新增 :root 变量 --type-content/#1878f3、--type-client/#8a63e8、--type-live/#f36b0f、--type-review/#138547 及各自 soft 色。行加 type-* 类驱动序号/竖条/chip 配色；状态 tag（待处理/进行中/已完成）保持原蓝橙绿。
- 标题下加一行颜色图例 .type-legend（内容/客户/直播/复盘 四色方块）。
- 第2行原时间列「张小北」改为「负责人 张小北」以明确是负责人而非时间。

## 修正：改错文件 + 在 今日任务.html 做类型配色
- 用户本意是改 `今日任务.html`（不是 indexnew.html）。已把 indexnew.html 的「今日任务」三处改动全部回退（:root 类型色变量、task-row CSS、HTML 行），保留此前认可的大小写/按钮反馈。
- `今日任务.html` 是枢纽+时间线结构，5 类任务（内容创作/直播运营/客户问询/线索洞察/数据复盘）枢纽节点已按类型配色，但时间线图标色错乱。改法（纯加效果，文案逻辑不动）：
  - 新增 CSS：`.timeline-item[data-category=X]` 设 `--cat`/`--cat-soft` 变量；`.timeline-task` 加 `border-left:4px solid var(--cat)` 彩色竖条；`.time-dot` 与 `.t-icon` 按类型上色（覆盖原 t-blue/t-orange/t-purple 错乱）；新增 `.cat-chip`（彩色类型标签）与 `.timeline-legend`（图例）。
  - 时间线标题下插入 `.timeline-legend` 五行色块图例。
  - 7 个静态时间线任务的 `<p>「时间 · 类型」` 拆成 `<p>时间</p><span class="cat-chip">类型</span>`。
  - JS `buildTimelineItem`(约1039行) 同步：去掉 p 里的「· 类型」，改为 cat-chip；动态项因 `item.dataset.category` 已设置，CSS 自动套用。
  - 注意：任务详情面板 detailTaskMeta 仍保留「时间 · 类型」文本（详情头部，未动）。

## 今日任务.html：logo 改可爱 + 整体排版调乖
- 仅改 CSS，未动任何文案/结构。
- logo：原「两个蓝色圆角竖条拼 m + 文字 mcn」改为圆角渐变 squircle 图标（48px, radius16, 渐变 #7db4ff→#3f86f6, 白字 "m"），文字 mcn 改柔蓝 #2b6fe0；hover 时图标上移+微旋 -5°。删除了蓝色覆盖块里旧的 `.brand-mark border-color` 规则（改注释占位）。
- 整体「乖乖」微调（新增 <style> 块）：app/card 圆角加大(26/24)且阴影更柔、卡片 hover 上浮；section-icon 改圆形；nav-item radius20 + 弹跳 hover/active 阴影；task-node radius18 hover 上浮；tool/select/btn radius13；profile 头像加白+蓝双描边。

## SOP管理_原型图.html：导航栏与 今日任务.html 完全一致
- 用户要求导航栏（含 logo 与头像）与 今日任务.html 像素级一致。
- 第一版：HTML 换为 今日任务同款（.brand 圆角渐变 m 图标 + .nav-item 今日任务[on]/数字分身/复盘助手 + .rail-spacer + .profile），CSS 同步（.rail/.brand/.brand-mark/.nav-item/.nav-icon/.rail-spacer/.profile），JS 高亮选择器 .rail-item→.nav-item。
- 修正：头像原用 SOP 的 吉祥物.jpg（沙箱缺图），改为用脚本原样提取 今日任务.html profile 内 base64 头像（alt="张三" 约 7.6KB data URI）写入 SOP，不再依赖外部文件；补齐 .nav-item hover/选中态 box-shadow+transform 对齐「乖乖」块。logo 此前已同款。临时脚本 copy_avatar.py 用后已删。
- 说明：active 项仍按 今日任务.html 默认=「今日任务」；若要在 SOP 页高亮「SOP 管理」或加回该项，告知即可。

## 今日任务.html：整体排版对齐 SOP（flex 骨架）
- 用户反馈 .board 整体排版奇怪，要求与 SOP 那边一致。
- 根因：.board 原是刚性三列网格（270/526/316），并被三段无 @media 的 <style> 覆盖块反复写死 798/726px 高度且 align-items 混乱。
- 改造：.board 改为 `display:flex; gap:14px; align-items:stretch`（对齐 SOP 的 .cols）；.column 固定宽 288px（对齐 SOP 侧栏 .list 宽）；.hub-card 固定 520px + min-height:760；.timeline-card `flex:1; min-height:760` 弹性占满其余宽度（对齐 SOP 的 .stage 主区）。
- 合并三段覆盖块为一段干净规则：保留 .timeline-body 独立滚动（flex:1; overflow-y:auto + webkit 滚动条样式），.calendar-card 固定 318px / .activity-card flex:1，删除所有 grid-template-columns 与固定高度。orbit 中心用 left:50% 居中，520px 宽下正常显示。
- 仅改 CSS 布局，未动文案/结构/交互逻辑；<1280 的 transform:scale 缩放仍生效。

## 今日任务.html：logo 改用 pictures/logo.png（去掉 mcn 文字）
- 用户要求工作台 logo 用真实图片 `pictures/logo.png`，且 logo 下方不再写 "mcn"。
- HTML：`.brand` 内原渐变 "m" 标记 + `<span>mcn</span>` 换成 `<img class="brand-logo" src="pictures/logo.png" alt="logo">`。
- CSS：删 `.brand-mark` 渐变与 "m" 伪元素、`.brand span` 文字样式；新增 `.brand-logo`（44×44, object-fit:contain, radius12, hover 微旋 -5°）。清理两处旧注释/无效规则。
- 关联：SOP 导航此前与 今日任务"完全一致"，仍用渐变 "m" 标记；用户随后确认要把 SOP 也同步。

## SOP管理_原型图.html：导航 logo + 头像同步今日任务.html
- 用户提醒 SOP 导航忘记跟今日任务同步（logo、头像两处）。
- logo：CSS `.brand-mark` 渐变+「m」伪元素+`.brand span` 删除，新增 `.brand-logo`（44×44, contain, radius12, hover 微旋）；HTML 原 `<div class="brand-mark">+<span>mcn</span>` 换成 `<img class="brand-logo" src="pictures/logo.png" alt="logo">`。
- 头像：左下 `.profile` 的 base64 内联图通过脚本整体替换为 `<img alt="头像" src="pictures/主1.jpg">`（此前 SOP 头像曾是 今日任务 的 base64 头像，已废弃）。
- 现 SOP 与 今日任务 导航 logo/头像完全一致；图片均相对引用同目录 `pictures/`。

## SOP管理_原型图.html：SOP 助手头像改为 pictures/clawbot.png
- 用户要求把对话区 SOP 助手头像（聊天气泡）从 `吉祥物.jpg` 换成 `pictures/clawbot.png`。
- 全局替换 4 处字面 `吉祥物.jpg`：3 处静态 `<img src>` + JS 变量 `var MASCOT`；JS 动态生成气泡（835/845 行）经 MASCOT 变量自动跟随。现已无 吉祥物.jpg 残留。

## indexnew.html：首页补充内容（概览数据条 + 快捷操作）
- 用户要求给工作台首页"搞点内容"。
- 在 hero 与三栏卡片之间新增两块（纯新增，未动原卡片）：
  1. `.overview` 概览数据条：4 个 KPI（今日发布12 / 总播放28.7万+3.2万↗ / 新增粉丝+1,263+268↗ / 待办9 含3项逾期↓），hover 上浮，绿/红 trend 配色。
  2. `.quick-actions` 快捷操作：胶囊按钮（新建内容/智能选题/数据复盘/客户跟进/一键群发），hover 变蓝上浮，点击 scale 弹动（脚本新增 .qa 监听）。
- CSS 用第一 :root 内联变量（--accent/--green/--shadow 等）保持一致；自带 @media(max-width:1279px) 降为 2 列 + 去左侧缩进。
- ⚠️ 后续用户要求"首页不要乱加东西"，已**全部撤销**：删除 .overview / .quick-actions 的 CSS 块、HTML 两段、以及 .qa 的 JS 点击监听。首页现仅保留 hero + 三个主 tab（今日任务/数字分身/复盘助手）。

## 今日任务_AI执行版.html：实时运行中心轨道节点去标签 + 修复遮挡 + 点击日志
- 用户反馈中间「实时运行中心」轨道节点卡片文字有遮挡，多余标签应去掉；后续要求点击节点后弹出灰色小字日志。
- 删除 5 个 `.task-node` 内的 `<span class="node-state">` 进度标签；grid-template-columns 从三列改两列（4 处媒体查询同步）。
- 新增 `.node-log-panel`（hub-area 底部绝对定位）：点击外围 5 个 task-node 后，动画结束追加一行日志（时间+名称+灰色小标签），slideIn 动画入场，最多保留 8 条。
